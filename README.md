# FeastFlow Gourmet Food Ordering
A complete Flask-SQLAlchemy full-stack college final-year project.

## Local Setups
1. Establish a visual environment: `python -m venv venv`
2. Enable dependencies: `pip install -r requirements.txt`
3. Fire up the backend: `python app.py`
